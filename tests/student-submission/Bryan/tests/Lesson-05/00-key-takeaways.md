## Function advance 
### Function expression : 
    const add = function(a,b)
    {
        return a+b;
    };
### Lambda function : sử dụng dấu =>
    VD: const add(a,b) => 
    {
        Return a+b;
    };
Hoặc ngắn gọn hơn:   const add(a,b) => a+b; 
### Anonymous function : được sử dụng khi function chỉ cần dùng 1 lần hoặc làm callback
    Function ()
## DOM 
    Overview về DOM
    Các thẻ DOM HTML thường gặp
## Selector
### Các loại selector thường dùng:
#### Xpath
    Tuyệt đối : đi dọc theo cây DOM , bắt đầu bằng 1 / 
    Tương đối : tìm dựa vào đặc tính, bắt đầu bằng 2// => nên dùng
        VD : //tenthe[@thuoctinh="giatri"]
#### CSS Selector
#### Playwright Selector
## Basic synctax
    Tương tác cơ bản : navigation,click ,fill,radio/checkbox,upload file,hover, confirmation dialog,...
