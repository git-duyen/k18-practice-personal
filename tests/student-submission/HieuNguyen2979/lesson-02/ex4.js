const myHeight = 190;
let iWeight = (myHeight -100) * 0.9;
let maxWeight = myHeight -100;
let minWeight = (myHeight -100) * 0.8;
console.log(`Cân nặng lý tưởng của bạn là ${iWeight} Cân nặng tối đa của bạn là ${maxWeight} Cân nặng tối thiểu của bạn là ${minWeight}`)