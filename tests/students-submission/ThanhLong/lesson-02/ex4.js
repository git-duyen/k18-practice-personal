const height = 170;
const lẻ = height % 100;
const weightRecommend = (lẻ * 9) / 10;
const weightMax = lẻ
const weightMin = (lẻ * 8) / 10;

console.log("weightRecommend:"+ weightRecommend, "weightMax:"+ weightMax, "weightMin:"+ weightMin);

