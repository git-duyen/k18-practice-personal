const myheight = 160;
const idealweight = (myheight - 100) * 9 / 10;
const maxweight = myheight - 100;
const minweight = maxweight * 8 / 10;;
console.log("idealweight:" + idealweight + " maxweight:" + maxweight + " minweight:" + minweight);